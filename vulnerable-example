// Example vulnerable code for CodeQL scanning

// Hardcoded secret (bad practice)
const apiKey = "12345-SECRET-API-KEY";

// Unsafe use of eval (vulnerability)
function runUserCode(userInput) {
    eval(userInput); // This is dangerous!
}

// Example usage
const userInput = "console.log('Hello from user input!')";
runUserCode(userInput);
